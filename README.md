# IT2_F_VenderProject2
作り直したリポジトリです

みんなでやるわよ。ここに次回用メモ。

共同編集者
：溝畑斗和
：松尾一輝
：咲間智明
：柳瀬尚也

5/18　メモ
次回、修正作業。ユーザーネームとメアドの設定をみんなの分ローカルに変更しますね。
追記　ターミナル上に以下を一文ずつ入力してください↓
（グローバル設定の消去）
 git config --global --unset user.name
 git config --global --unset user.name
（ローカルでの設定）
 git config user.name "ユーザーネーム"
 git config user.email "自身のemail（@meitec-grp.comのやつ）"
入力後、以下を入力して、入力したユーザーネームとメアドが表示されたら完了です。
 git config --local -l
（以下はUI状から設定を確認する方法）
１，ツールタブからオプションを開く
２，左のリストからソース管理＞Git設定＞Gitグローバル構成
３，欄内のユーザ名とメアドを消去し保存
４，その後、＞Gitリポジトリの構成を開く
５，右欄内のユーザ名とメアドの入力で保存でおｋ


5/19　メモ
・110円の追加→計算、払出の実装（尚也　完了済み）
・上限1990円の設定実装（尚也　完了済み）
・金額不足時のエラーメッセージ（尚也　完了済み）
・入力不可文字のエラーメッセージ（尚也、咲間担当）
・売り上げ記録用のcsv書き込み機能の実装（松尾担当）
（・在庫補充用機能の実装（暇人担当））

追記　ブランチモデル「Gitフロー」での作業する手順について
（base branchの名前をdevelop branchに変更しました）
mainをそのまま編集するわけにはいかないので、編集する作業場を分ける、といった考え方です。
mainはいつでもリリース可能にして、developが全員の編集内容をmerge（合流）させる場所とします。
皆さんがこれから編集作業するときには、developbranchをbaseとしてbranchを新しく作成して、そのうえで編集作業を行ってください。
その後、その自分用編集branchをdevelop branchにmergeすることで、全員の編集内容をまとめていくって寸法です。
ので、develop元でbranchを作って作業するようにしてください。（brunch名に、誰が何を編集しているのかわかるようにしてくださいね。）

・110円の追加→計算、払出の実装（10:49追加　やなせ）
・上限1990円の設定実装（10:49追加　やなせ）
・金額不足時のエラーメッセージ（10:49追加　やなせ）
・エラーメッセージ表示時間延長（10:49追加　やなせ）